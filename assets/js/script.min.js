$((function(){$(".fancybox").fancybox({helpers:{title:{type:"inside"}}})})),$(document).ready((function(){$(".collapse.show").each((function(){$(this).prev(".card-header").find(".fas").addClass("fa-minus").removeClass("fa-plus")})),$(".collapse").on("show.bs.collapse",(function(){$(this).prev(".card-header").find(".fas").removeClass("fa-plus").addClass("fa-minus")})).on("hide.bs.collapse",(function(){$(this).prev(".card-header").find(".fas").removeClass("fa-minus").addClass("fa-plus")}))}));



// /* Menu on Scrolldown
//  * ------------------------------------------------------ */
// var clMenuOnScrolldown = function() {
// 	var hdr = $('.navigation'),
// 		hdrTop = $('.navigation').offset().top;
// 	$(window).on('scroll', function() {
// 		if($(window).scrollTop() > hdrTop) {
// 			hdr.addClass('sticky');
// 		} else {
// 			hdr.removeClass('sticky');
// 		}
// 	});
// };
$('.stdtest').owlCarousel({
		loop: true,
		margin: 30,
		nav: false,
		navText: ['<i class="icon-arrow-left"></i>', '<i class="icon-arrow-right"></i>'],
		dots: true,
		autoplay: true,
		center: true,
		responsiveClass: true,
		responsive: {
			0: {
				items: 1
			},
			1024: {
				items: 3
			}
		}
	})
	// Porfolio isotope and filter
$(window).on('load', function() {
	var portfolioIsotope = $('.portfolio-container').isotope({
		itemSelector: '.portfolio-item',
		layoutMode: 'fitRows'
	});
	$('#portfolio-flters li').on('click', function() {
		$("#portfolio-flters li").removeClass('filter-active');
		$(this).addClass('filter-active');
		portfolioIsotope.isotope({
			filter: $(this).data('filter')
		});
		aos_init();
	});
});
// Initiate venobox (lightbox feature used in portofilo)
$(document).ready(function() {
	$('.venobox').venobox();
});